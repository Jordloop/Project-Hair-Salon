@{
  Layout="Shared/_Layout.cshtml";
}

<h1>All Stylist</h1><br>

<ul>
  @if (Model.Count == 0)
  {
    <p>You have no Stylist. Click "Add New Stylist" button above to add a new stylist.</p>
  }
  @foreach (var restaurant in Model)
  {
  <li>Stylist.GetName()</li>
  }

</ul>
