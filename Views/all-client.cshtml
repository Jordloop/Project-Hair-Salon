@{
  Layout="Shared/_Layout.cshtml";
}

<h1>All Clients</h1>

@if (Model.Count !=0)
{
  <ul>
    @foreach (var client in Model)
    {
      <li>@client.GetName()</li>
    }
  </ul>
}
else
{
  <p>You have no Cleints.</p>
}
<a href="/client/add">Add a client.</a>
